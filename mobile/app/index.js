import { Redirect } from 'expo-router';
import React from 'react';
import App from '../App';

export default function Index() {
  // This is the entry point for the app when using expo-router
  // We're just rendering the main App component here
  return <App />;
}
